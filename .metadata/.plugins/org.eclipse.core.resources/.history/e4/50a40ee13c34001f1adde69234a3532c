package Ch06_class_sec12;

public class Constructor {
	private int x;
	private int y;
	
	//======================================
	//생성자 메서드: 객체 생성될 때 한번 호출되는 메서드, 생성될 때 제외, 절대 호출이 불가능하다.
	//클래스와 이름이 동일하고, 반환형이 없다.
	//역할: 필드 값 초기화
	
	//개발자가 추가한 명시적 생성자: 개발자가 추가하지 않으면, JVM이 자동으로 추가해준다.
	public Constructor () {
		x = 100;
	}
	
}
